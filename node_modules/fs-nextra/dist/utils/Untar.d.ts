/// <reference types="node" />
import Header from './Header';
import { Writable } from 'stream';
export default class Untar extends Writable {
    private header;
    private file;
    private totalRead;
    private recordSize;
    private queue;
    _write(data: Buffer, encoding: string, next: Function): void;
    private slice;
    private send;
    private next;
    files(): AsyncIterableIterator<{
        header: Header;
        file: Buffer;
    }>;
}
