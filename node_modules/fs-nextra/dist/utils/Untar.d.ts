/// <reference types="node" />
import { Writable } from 'stream';
import { Header } from './Header';
export declare class Untar extends Writable {
    private header;
    private file;
    private totalRead;
    private recordSize;
    private queue;
    _write(data: Buffer, encoding: string, next: Function): void;
    private slice;
    private next;
    files(): AsyncIterableIterator<{
        header: Header;
        file: Buffer;
    }>;
}
