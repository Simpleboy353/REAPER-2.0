/// <reference types="node" />
import { WriteOptions } from './writeFileAtomic';
/**
 * Writes a file to disk, creating all directories needed to meet the filepath provided.
 * @function outputFile
 * @memberof fsn/nextra
 * @param file The path to the file you want to create
 * @param data The data to write to file
 * @param options The write options or the encoding string.
 * @param atomic {description}
 */
export default function outputFile(file: string, data: string | Buffer | Uint8Array, atomic?: boolean): Promise<void>;
export default function outputFile(file: string, data: string | Buffer | Uint8Array, options?: WriteOptions | string, atomic?: boolean): Promise<void>;
